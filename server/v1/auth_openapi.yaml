# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi
openapi: 3.0.3
info:
    title: Authorization API Reference
    description: |4-
        ## Overview
        This section is organized around Tigris authorization HTTP APIs. The APIs accepts JSON requests and returns JSON-encoded responses. The APIs conforms to standard HTTP status codes.
    license:
        name: Apache License
        url: https://github.com/tigrisdata/tigris/blob/main/LICENSE
    version: 0.0.1
paths:
    /auth/token:
        post:
            tags:
                - Auth
                - auth
            summary: 'Gets an access token using refresh token. Supports two grant_type: [refresh_token, client_credentials] - when refresh_token is used only refresh_token as request is required, for the client_credentials [client_id, client_secret] are required'
            description: |-
                Gets an access token.
                 supports two grant_type: [refresh_token, client_credentials]
            operationId: Auth_GetAccessToken
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetAccessTokenRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetAccessTokenResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GetAccessTokenRequest:
            type: object
            properties:
                grant_type:
                    enum:
                        - REFRESH_TOKEN
                        - CLIENT_CREDENTIALS
                    type: string
                    format: enum
                refresh_token:
                    type: string
                    description: 'case: refresh_token: A valid refresh token'
                client_id:
                    type: string
                    description: 'case: client_credentials: A valid credentials (client_id, client_secret, audience)'
                client_secret:
                    type: string
            description: Requests the access token by specifying grant_type:refresh_token & refresh_token or grant_type:client_credentials & [client_id, client_secret]
        GetAccessTokenResponse:
            type: object
            properties:
                access_token:
                    type: string
                    description: An access token.
                refresh_token:
                    type: string
                    description: Refresh token.
                expires_in:
                    type: integer
                    description: Access token expiration timeout in seconds
                    format: int32
            description: GetAccessToken returns response containing access_token and optionally refresh_token
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
    securitySchemes:
        BearerAuth:
            type: http
            scheme: bearer
security:
    - BearerAuth: []
tags:
    - name: Auth
    - name: application
      description: Tigris supports application based authentication. This section provides API to manage user applications
    - name: auth
      description: Tigris supports pluggable oauth provider. The auth section of API provides open authorization (OAuth 2.0) APIs
